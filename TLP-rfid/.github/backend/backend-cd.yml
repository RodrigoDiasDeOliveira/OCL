name: Backend CD

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: backend-jar

      - name: Deploy
        run: |
          echo "Deploying backend..."
          echo "Aqui você conecta com OCI / Azure / Docker / VM"